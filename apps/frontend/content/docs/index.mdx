---
title: Project Overview
description: A modern, high-performance monorepo boilerplate for building scalable web applications.
---

## 1. Architecture

<Callout title="Modern Tech Stack" type="info">
  This boilerplate uses the latest versions of Next.js, NestJS, and Prisma, optimized for performance and type safety.
</Callout>

The project is organized as a monorepo using [Turborepo](https://turbo.build/) and [pnpm](https://pnpm.io/) workspaces. Documentation is independent and accessible at `/docs` using [Fumadocs](https://fumadocs.dev/).

<Cards>
  <Card title="Frontend" icon={<LayoutIcon />}>
    Built with [Next.js 16+](https://nextjs.org/), [React 19](https://react.dev/), [Tailwind CSS v4](https://tailwindcss.com/), and [Shadcn/UI](https://ui.shadcn.com/).
    
    [Explore Frontend Docs →](/docs/frontend)
  </Card>
  <Card title="Backend" icon={<ServerIcon />}>
    [NestJS 11](https://nestjs.com/) REST API powered by [Prisma ORM](https://www.prisma.io/) and [PostgreSQL](https://www.postgresql.org/).
    
    [Explore Backend Docs →](/docs/backend)
  </Card>
  <Card title="Shared" icon={<BoxIcon />}>
    Centralized [TypeScript](https://www.typescriptlang.org/) types, DTOs, and utilities in @repo/shared.
    
    [Learn about Monorepo Integration →](/docs/backend#4-monorepo-integration)
  </Card>
  <Card title="Infrastructure" icon={<DatabaseIcon />}>
    Orchestrated with [Docker Compose](https://docs.docker.com/compose/) for database and services.
    
    [View Deployment Guide →](/docs/backend#3-development)
  </Card>
</Cards>

## 2. AI Agent & Living Documentation

This boilerplate is designed to be **AI-First**. It includes a [Living Documentation](/docs/ai-guide) workflow that ensures code and documentation stay in sync.

- **Agent Context**: Optimized for [Gemini CLI](https://github.com/google-gemini/gemini-cli) via the `.gemini/` folder.
- **Rules & Skills**: Custom instructions guide AI agents through complex tasks.
- **Sync Verification**: Use the `doc-check` tool to ensure architectural changes are documented.

## 3. Getting Started

<Accordions>
  <Accordion title="Prerequisites">
    Ensure you have the following installed:
    - [Node.js](https://nodejs.org/) (Latest LTS)
    - [pnpm](https://pnpm.io/) `v10.28.1`
    - [Docker](https://www.docker.com/) (for database)
  </Accordion>

  <Accordion title="Installation">
    Clone the repository and install dependencies:

    ```bash
    pnpm install
    ```
  </Accordion>

  <Accordion title="Running the Project">
    <Tabs items={['Local Development', 'Full Docker']}>
      <Tab value="Local Development">
        Start the database: 
        ```bash
        docker compose up db -d
        ```
        Run dev servers: 
        ```bash
        pnpm dev
        ```
      </Tab>
      <Tab value="Full Docker">
        Run the entire stack inside containers:
        ```bash
        docker compose up
        ```
      </Tab>
    </Tabs>
  </Accordion>
</Accordions>

## 4. Key Commands

| Command | Description |
| :--- | :--- |
| `pnpm dev` | Start all apps in development mode. |
| `pnpm build` | Build all packages and applications. |
| `pnpm test` | Run all tests across the monorepo. |
| `pnpm lint` | Run [ESLint](https://eslint.org/) across the monorepo. |
| `pnpm turbo check-types` | Run global TypeScript type checking. |
| `pnpm doc-check` | Verify if documentation is in sync with code changes. |
| `pnpm --filter backend prisma:migrate` | Run [Prisma](https://www.prisma.io/) migrations. |

## 5. Development Conventions

<Callout title="Strict Typing" type="info">
  Mandatory strict mode for TypeScript.
</Callout>

<Callout title="No Prettier" type="info">
  Formatting and linting are handled exclusively by ESLint.
</Callout>

<Callout title="Shared Code" type="info">
  All common interfaces and DTOs MUST reside in `@repo/shared`.
</Callout>

---

<Callout title="Ready to build?" type="warn">
  Check our [AI Guide](/docs/ai-guide) to see how to collaborate with AI agents effectively.
</Callout>